type Query {
    feed: [Post!]!
    post(id: ID!): Post
}

type Mutation {
    createPost(url: String!, title: String!): Post!
    updatePost(id: ID!, url: String, title: String): Post
    deletePost(id: ID!): Post
}

type Post {
    id: ID!
    title: String!
    url: String!
}